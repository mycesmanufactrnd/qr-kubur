export enum ActiveInactiveStatus {
  ACTIVE = "active",
  INACTIVE = "inactive"
}

export enum ApprovalStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REJECTED = "rejected",
}

export enum VerificationStatus {
  PENDING = "pending",
  VERIFY = "verify",
  REJECTED = "rejected",
}